# Linux学习练习题

## 第一章：入门基础题

### 1.1 基础概念题

1. **Linux操作系统的核心开发者是谁？Linux最初是在哪个大学开发的？**

2. **简述Linux与Windows在以下几个方面的区别：**
   - 软件授权与支持
   - 安全性
   - 使用习惯
   - 应用场景

3. **目前主流的Linux发行版有哪些？请至少列举5个。**

### 1.2 目录结构题

4. **请说明以下Linux核心目录的作用：**
   - /bin
   - /sbin
   - /etc
   - /home
   - /root
   - /var
   - /usr

5. **在Linux中，设备文件通常存放在哪个目录下？**

### 1.3 实操题（需要Linux环境）

6. **在你的Linux系统中执行以下操作：**
   - 查看当前工作目录的绝对路径
   - 列出当前目录下的所有文件（包括隐藏文件）
   - 查看根目录下的内容

---

## 第二章：VI/VIM编辑器

### 2.1 模式操作题

1. **VIM有哪三种核心模式？请描述它们之间的切换方式。**

2. **在一般模式下，如何执行以下操作？**
   - 复制当前行
   - 删除当前行
   - 粘贴刚才复制的内容
   - 撤销上一步操作
   - 移动到文档开头
   - 移动到文档末尾

3. **在一般模式下，如何进入插入模式？请列举至少3种方式及其区别。**

### 2.2 指令模式题

4. **在指令模式下，如何执行以下操作？**
   - 保存文件
   - 保存并退出
   - 不保存强制退出
   - 显示行号
   - 取消行号显示

5. **请写出在VIM中替换文本的命令：**
   - 替换当前行第一个匹配的"old"为"new"
   - 替换当前行所有匹配的"old"为"new"
   - 替换文档中每一行第一个匹配的"old"为"new"
   - 替换文档中所有匹配的"old"为"new"

### 2.3 实操题

6. **使用VIM创建一个名为`test.txt`的文件，并完成以下操作：**
   - 输入3行文本："Hello Linux"、"Hello World"、"Hello VIM"
   - 复制第二行并粘贴到文件末尾
   - 删除第一行
   - 将所有"Hello"替换为"Hi"
   - 保存并退出

---

## 第三章：网络配置与系统管理

### 3.1 网络基础知识题

1. **请解释虚拟机网卡的三种连接模式及其特点：**
   - 桥接模式
   - NAT模式
   - 仅主机模式

2. **在CentOS 7中，如何查看和配置网络IP地址？请写出主要步骤。**

3. **如何查看和修改Linux系统的主机名？请写出两种方法。**

### 3.2 系统管理题

4. **请解释什么是进程，什么是服务。**

5. **在CentOS 7中，如何使用systemctl命令执行以下操作？**
   - 查看服务状态
   - 启动服务
   - 停止服务
   - 重启服务
   - 设置服务开机自启
   - 禁止服务开机自启

6. **Linux有哪些传统运行级别？CentOS 7简化为哪两个目标？**

7. **请写出以下关机重启命令的功能：**
   - sync
   - halt
   - poweroff
   - reboot
   - shutdown now

### 3.3 实操题

8. **在你的Linux系统中完成以下操作：**
   - 查看当前主机名
   - 修改主机名为"mylinux"（任选一种方法）
   - 查看防火墙服务状态
   - 临时关闭防火墙
   - 将内存数据同步到硬盘

---

## 第四章：常用基本命令

### 4.1 帮助命令题

1. **如何使用man命令查看命令的帮助信息？请举例。**

2. **什么是内置命令和外部命令？如何判断一个命令是内置命令还是外部命令？**

3. **请写出以下常用快捷键的功能：**
   - Ctrl + C
   - Ctrl + L
   - Tab
   - 上下箭头

### 4.2 文件目录类命令题

4. **请写出以下操作的命令：**
   - 显示当前工作目录的绝对路径
   - 列出当前目录下所有文件的详细信息（包括隐藏文件）
   - 切换到当前用户的家目录
   - 切换到上一级目录
   - 切换到上一次所在目录
   - 递归创建多级目录`a/b/c/d`
   - 创建空文件`test.txt`
   - 复制文件`file1.txt`为`file2.txt`
   - 递归复制目录`dir1`为`dir2`
   - 删除文件`test.txt`
   - 递归删除目录`dir1`（不提示确认）
   - 重命名文件`old.txt`为`new.txt`
   - 移动文件`file.txt`到`/tmp`目录

5. **如何查看文件内容？请列举至少3种命令及其适用场景。**

6. **请写出以下操作的命令：**
   - 查看文件前5行
   - 查看文件最后10行
   - 实时监听文件内容变化
   - 将`ls -l`的输出覆盖写入`file.txt`
   - 将`echo "hello"`的输出追加到`file.txt`末尾

7. **什么是软链接和硬链接？它们有什么区别？如何创建软链接？**

8. **如何查看历史执行过的命令？如何清空历史命令？**

### 4.3 实操题

9. **在你的Linux系统中完成以下操作：**
   - 创建目录`mydir`
   - 在`mydir`目录下创建文件`file1.txt`和`file2.txt`
   - 查看`mydir`目录的详细内容
   - 将`file1.txt`复制为`file3.txt`
   - 将`file2.txt`重命名为`file4.txt`
   - 创建`file3.txt`的软链接`file3_link`
   - 查看所有历史命令

---

## 第五章：时间日期与用户管理

### 5.1 时间日期类命令题

1. **请写出以下操作的命令：**
   - 显示当前完整时间
   - 以"YYYY-MM-DD HH:MM:SS"格式显示当前时间
   - 显示当前年份
   - 显示当前月份
   - 显示当前日期
   - 显示当前小时（24小时制）
   - 显示当前分钟
   - 显示当前秒数
   - 显示当前时间戳
   - 查看本月日历
   - 查看2025年全年日历

2. **如何设置系统时间？如何同步网络时间？**

### 5.2 用户管理类命令题

3. **请写出以下操作的命令：**
   - 创建用户`zhangsan`
   - 创建用户`lisi`并指定家目录为`/home/lisi_home`
   - 设置用户`zhangsan`的密码
   - 查看用户`zhangsan`的信息
   - 查看系统所有用户
   - 切换到用户`zhangsan`
   - 退出当前用户，返回上一级用户
   - 查看当前登录用户
   - 删除用户`lisi`（保留家目录）
   - 彻底删除用户`zhangsan`（包括家目录）

4. **如何给普通用户赋予root权限？请写出主要步骤。**

### 5.3 用户组管理类命令题

5. **请写出以下操作的命令：**
   - 创建用户组`dev`
   - 修改用户组`dev`的名称为`develop`
   - 删除用户组`develop`
   - 将用户`zhangsan`添加到`dev`组

### 5.4 实操题

6. **在你的Linux系统中完成以下操作：**
   - 显示当前时间，格式为"2025-05-20 14:30:00"
   - 查看本月日历
   - 创建用户`testuser`
   - 设置`testuser`的密码
   - 切换到`testuser`用户
   - 查看当前用户
   - 退出`testuser`用户
   - 删除`testuser`用户

---

## 第六章：文件权限与搜索查找

### 6.1 文件权限类命令题

1. **请解释文件属性的10个字符的含义，以`-rw-rw-r--`为例。**

2. **请说明r、w、x权限对于文件和目录的不同含义。**

3. **什么是文件的属主、属组和其他用户？**

4. **请用符号法写出以下权限设置命令：**
   - 给文件属主添加执行权限
   - 给所有用户添加执行权限
   - 移除属组的写权限
   - 给所有用户设置读写执行权限

5. **请用数字法表示以下权限：**
   - rwxr-xr-x
   - rw-r--r--
   - rwxrwxrwx
   - r-xr-xr--

6. **请写出以下操作的命令：**
   - 将文件`file.txt`的权限设置为755
   - 递归将目录`mydir`的权限设置为777
   - 将文件`file.txt`的所有者修改为`zhangsan`
   - 递归将目录`mydir`的所有者修改为`zhangsan`
   - 将文件`file.txt`的属组修改为`dev`

### 6.2 搜索查找类命令题

7. **请写出以下操作的命令：**
   - 在当前目录及子目录中查找文件`nginx.conf`
   - 在`/root`目录及子目录中查找文件`nginx.conf`
   - 查找当前目录及子目录中所有的`.txt`文件
   - 查找`/home`目录下属于用户`zhangsan`的所有文件
   - 查找当前目录下大于1M的文件
   - 查找当前目录下小于100k的文件

8. **locate命令和find命令有什么区别？使用locate命令需要注意什么？**

9. **什么是管道符？请举例说明。**

10. **请写出以下操作的命令：**
    - 查找文件`nginx.conf`中包含`location`的行，并显示行号
    - 查看`/home`目录中包含`test`的文件
    - 查看系统中包含`java`的进程
    - 统计文件`app.log`中包含`error`的行数

### 6.3 实操题

11. **在你的Linux系统中完成以下操作：**
    - 创建文件`test.txt`，并写入一些内容
    - 查看`test.txt`的权限
    - 将`test.txt`的权限设置为755
    - 在当前目录及子目录中查找所有的`.txt`文件
    - 使用grep在`test.txt`中查找指定内容

---

## 第七章：压缩解压与磁盘管理

### 7.1 压缩解压类命令题

1. **请写出以下操作的命令：**
   - 使用gzip压缩文件`file.txt`
   - 使用gunzip解压文件`file.txt.gz`
   - 使用zip压缩目录`mydir`为`mydir.zip`
   - 使用unzip解压`mydir.zip`到`/tmp`目录
   - 使用tar打包压缩文件`file.txt`为`file.tar.gz`
   - 使用tar打包压缩目录`mydir`为`mydir.tar.gz`
   - 使用tar解压`mydir.tar.gz`到`/tmp`目录
   - 查看`mydir.tar.gz`的内容（不解压）

2. **gzip、zip、tar三种压缩方式各有什么特点？**

### 7.2 磁盘管理类命令题

3. **请写出以下操作的命令：**
   - 查看当前目录总大小（人性化显示）
   - 查看当前目录一级子目录大小
   - 查看所有文件系统磁盘使用情况（人性化显示）
   - 查看设备挂载情况
   - 查看设备挂载详细信息（包括文件系统类型）

4. **什么是挂载？如何挂载和卸载设备？**

5. **如何设置开机自动挂载设备？**

6. **如何查看磁盘分区情况？如何管理磁盘分区？**

### 7.3 实操题

7. **在你的Linux系统中完成以下操作：**
   - 创建目录`testdir`，并在其中创建几个文件
   - 使用tar将`testdir`打包压缩为`testdir.tar.gz`
   - 查看`testdir.tar.gz`的内容
   - 将`testdir.tar.gz`解压到`/tmp`目录
   - 查看当前目录总大小
   - 查看所有文件系统磁盘使用情况

---

## 第八章：进程管理与Shell脚本

### 8.1 进程管理类命令题

1. **请写出以下操作的命令：**
   - 查看所有进程（含详细信息）
   - 查看进程及父子关系
   - 过滤查找`java`进程
   - 通过PID终止进程
   - 强制终止进程
   - 通过进程名终止所有相关进程
   - 查看进程树（显示PID）
   - 实时监控系统进程状态

2. **请解释ps aux输出中以下字段的含义：**
   - USER
   - PID
   - %CPU
   - %MEM
   - STAT
   - COMMAND

3. **请解释进程状态STAT中常见字符的含义：**
   - R
   - S
   - T
   - Z
   - s
   - l
   - +
   - <
   - N

### 8.2 实操题

4. **在你的Linux系统中完成以下操作：**
   - 查看所有进程
   - 查找ssh相关进程
   - 查看进程树
   - 实时监控系统进程状态（观察几秒后退出）

---

## 第九章：综合应用题

### 9.1 情景应用题

1. **你是一名系统管理员，需要完成以下任务：**
   - 创建一个用户`developer`，并设置密码
   - 创建一个用户组`devgroup`，将`developer`添加到该组
   - 创建目录`/data/project`，设置所有者为`developer`，属组为`devgroup`
   - 设置`/data/project`目录的权限为775
   - 在`/data/project`目录下创建文件`README.md`
   - 备份`/data/project`目录为`project_backup.tar.gz`

2. **你需要分析系统日志文件`/var/log/messages`，请完成以下操作：**
   - 查看日志文件的最后50行
   - 实时监听日志文件的变化
   - 查找日志文件中包含`error`的行
   - 统计日志文件的行数

3. **系统磁盘空间不足，你需要清理空间，请完成以下操作：**
   - 查看磁盘使用情况
   - 查看哪个目录占用空间最大
   - 查找系统中大于100M的文件
   - 清理`/tmp`目录下的所有文件

### 9.2 综合实操题

4. **完成以下综合任务：**
   - 创建用户`student`
   - 切换到`student`用户
   - 在`student`的家目录下创建目录`workspace`
   - 在`workspace`目录下创建文件`notes.txt`，并写入内容
   - 复制`workspace`目录为`workspace_backup`
   - 将`workspace_backup`打包压缩为`workspace_backup.tar.gz`
   - 切换回root用户
   - 将`workspace_backup.tar.gz`移动到`/opt`目录
   - 查看`/opt`目录的内容
   - 清理测试文件和目录

---

## 第十章：Shell脚本入门

### 10.1 Shell脚本基础题

1. **什么是Shell脚本？Shell脚本的文件扩展名通常是什么？**

2. **如何创建和执行一个Shell脚本？**

3. **请解释以下Shell脚本中的特殊变量：**
   - $0
   - $1, $2, ...
   - $#
   - $*
   - $@
   - $?
   - $$

4. **请写出一个简单的Shell脚本，输出"Hello, World!"。**

5. **请写出一个Shell脚本，接受一个参数（用户名），输出"Hello, [用户名]!"。**

### 10.2 Shell脚本实操题

6. **编写一个Shell脚本`backup.sh`，功能如下：**
   - 接受一个参数（要备份的目录）
   - 将指定目录打包压缩为`backup_YYYYMMDD_HHMMSS.tar.gz`格式
   - 将备份文件保存到`/backup`目录
   - 输出备份完成信息

7. **编写一个Shell脚本`system_info.sh`，功能如下：**
   - 显示当前时间
   - 显示当前用户
   - 显示当前工作目录
   - 显示系统主机名
   - 显示磁盘使用情况
   - 显示内存使用情况

---

## 答案提示

(注：实际使用时，建议先自己思考和尝试，再查看学习笔记中的相关内容或使用man命令获取帮助。)

---

## 学习建议

1. **循序渐进**：按照章节顺序逐步学习和练习
2. **多动手实操**：Linux学习最重要的是实际操作
3. **善用帮助命令**：遇到问题时多使用man和help命令
4. **做好笔记**：记录重要的命令和遇到的问题
5. **持续练习**：定期回顾和练习，巩固所学知识

祝学习顺利！
